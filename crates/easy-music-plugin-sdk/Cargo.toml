[package]
name = "easy-music-plugin-sdk"
version.workspace = true
edition.workspace = true
license.workspace = true
description = "Plugin SDK for EasyMusic: manifest types, hooks, permissions, and host traits."

[dependencies]
serde = { version = "1", features = ["derive"] }
serde_json = "1"
