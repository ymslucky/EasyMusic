{
  "name": "easymusic",
  "private": true,
  "version": "0.1.0",
  "description": "Cross-platform desktop music app — Tauri + Next.js + Rust",
  "scripts": {
    "dev": "npm --prefix frontend run dev",
    "build": "npm --prefix frontend run build",
    "tauri": "tauri",
    "tauri:dev": "tauri dev",
    "tauri:build": "tauri build"
  },
  "devDependencies": {
    "@tauri-apps/cli": "^2"
  }
}
